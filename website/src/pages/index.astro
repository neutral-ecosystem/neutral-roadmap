---
import Layout from '../layouts/Layout.astro';
const sections = [
  { number: '01', label: 'Language', prefix: '/language/', accent: 'blue', description: 'Language contracts, architecture, decisions, and portable specifications.' },
  { number: '02', label: 'Editor', prefix: '/editor/', accent: 'sage', description: 'Capability-driven visual authoring and editor compliance.' },
  { number: '03', label: 'Flow', prefix: '/flow/', accent: 'warm', description: 'Execution architecture, requirements, and delivery roadmap.' },
  { number: '04', label: 'Repository rules', prefix: '/rules/', accent: 'violet', description: 'Documentation structure, portability, hosting, and publication policy.' },
];
---
<Layout title="Documentation" description="Neutral ecosystem documentation, generated from canonical Markdown.">
  <section class="home-hero shell">
    <div class="home-hero-copy">
      <p class="eyebrow">Neutral ecosystem</p>
      <h1>Documentation</h1>
      <p class="lede">One place for the contracts, decisions, and implementation paths that shape Neutral.</p>
      <div class="home-hero-meta" aria-label="Documentation status">
        <span>Canonical Markdown</span>
        <span>Portable versions</span>
      </div>
    </div>
    <div class="home-hero-mark" aria-hidden="true">
      <img src="/neutral-logo.png" alt="" width="112" height="112" />
      <span>Read the source.<br />Build the boundary.</span>
    </div>
  </section>
  <section class="directory" aria-labelledby="directory-title">
    <div class="shell">
      <div class="directory-heading">
        <div>
          <p class="eyebrow">Explore the system</p>
          <h2 id="directory-title">Choose a workstream</h2>
        </div>
        <p class="directory-intro">Each area has a project contract and versioned portable documentation.</p>
      </div>
      <div class="directory-list">
        {sections.map((section) => (
          <a class={`directory-row accent-${section.accent}`} href={section.prefix}>
            <span class="directory-number">{section.number}</span>
            <span class="directory-copy">
              <strong>{section.label}</strong>
              <span>{section.description}</span>
            </span>
            <span class="directory-open">Open <b aria-hidden="true">↗</b></span>
          </a>
        ))}
      </div>
    </div>
  </section>
</Layout>
