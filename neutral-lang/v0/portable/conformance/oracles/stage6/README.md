<!-- SPDX-License-Identifier: Apache-2.0 -->

# Stage 6 conformance oracles

This directory owns exact expected outcomes for captured vocabulary bundles and,
their source-language integration. Slice 6.1 records exact bundle
byte identities, normalized logical contracts, and bounded hostile-decoder error
classes without performing acquisition or code loading.

Slice 6.2 activates the exact captured `use` boundary, qualified nominal types,
closed payload validation, vocabulary-default provenance, IR/derivation facts,
and reader/probe traversal. Source fixtures cannot initiate acquisition.
