<!-- SPDX-License-Identifier: Apache-2.0 -->

# Positive closed-default fixtures

This directory contains source programs that Slice 4.2 must accept: every
required/defaulted and nullable/non-nullable field state, explicit overrides,
and recursively closed record defaults.

Within the ecosystem these immutable inputs connect omission semantics to
materialized logical IR, field provenance, validated reader views, probe
summaries, and Stage 4 conformance evidence.
