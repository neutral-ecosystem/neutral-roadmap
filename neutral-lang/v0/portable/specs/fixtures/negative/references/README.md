<!-- SPDX-License-Identifier: Apache-2.0 -->

# Negative typed identity-reference fixtures

These fixtures freeze Stage 5.2 rejection behavior for unknown targets,
record-declaration targets, non-exact target types, and use of an ordinary value
where an identity constructor is required. Every failure is reference/type
validation only and produces no authoritative IR.
